# R1 Driver Judgment

Panel calls: Antigravity completed; Claude Code failed with an expired OAuth
token (HTTP 401). No Claude claims exist to ground. The Codex UI remains the
anchor and sole judge.

## Accepted

- CONFIRMED `[P6]`: remove the conditional LTX T2V rerun. The receipt/log chain
  already exhausts the three-attempt budget, and selected B is human-pending.
- CONFIRMED `[P5]`: distinguish the matched-audio diagnostic artifact from the
  original-source delivery preview and define the media contract explicitly.
  The panel's proposed removal of the source preview was not accepted.
- CONFIRMED `[P7]`: identify W0, bind order/roles to the manifest, and state
  abort/lock/server cleanup behavior.
- CONFIRMED `[P8/P9]`: a pending Mini Mime human gate defers R2V but can be
  reported truthfully; it need not prevent `SESSION_REPORT.md` or a local
  commit.

## Rejected as misreads

- MISREAD `[P5]`: external source muxing is not accidental double muxing. The
  user explicitly requires real source audio for delivery; it is a separate
  named artifact whose copied video hash must match the conditioning diagnostic.
- MISREAD `[P7]`: H3 T2V best does not use Gemma; checked recipe node 2 uses
  Qwen3-VL. Cache retention is the phenomenon the sentinels measure. Calling an
  unverified `/free` endpoint before each candidate would erase that evidence.
- MISREAD `[P4]`: the Kibitz and Codex review gates were explicitly requested
  by Jeffrey and cannot be cut as over-engineering.
- REJECTED `[P7]`: S1/S2 are necessary localization points around model-family
  transitions. Cutting them would weaken the requested creep diagnosis.

## Verify at build

- Owned 8199 live schema must accept dotted dynamic-combo inputs.
- Suite identity, locking, archive monotonicity, and shutdown confirmation need
  focused tests before any suite render.
- DWM jitter is expected to stay within 0.25 GiB based on existing H3 pairs;
  retain it as measured local evidence, not a general hardware guarantee.
