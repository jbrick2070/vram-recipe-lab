VERDICT: build-ready as-is? no. Multiple architectural contradictions in campaign termination, double-muxing audio preview pipelines, unmanaged VRAM cache accumulation across suite runs, and un-gated human loop stalls in automated completion.

MUST-FIX BEFORE BUILD:
1. [P6] Contradictory LTX T2V Campaign Close-Out & Unbudgeted VRAM Rerun: P1 states the primary outcome is to "close the exhausted LTX T2V campaign honestly", and P6 notes that the 3-attempt allowance is exhausted with no lawful 4th quality topology. However, P6 introduces an ambiguous conditional exception: "Close the campaign in docs/ESCALATE.md unless Jeffrey explicitly approves that exact artifact for a reserve-12 certification rerun." This creates an unresolved execution state and contradicts P2's non-negotiable 14.5 GiB VRAM ceiling by contemplating a rerun of cell B (previously measured at 15.04 GiB unreserved). Fix: Remove the conditional approval loophole in P6. Hard-close LTX T2V in docs/ESCALATE.md as an exhausted 15.04 GiB artifact without scheduling any rerun in this campaign.
2. [P5] Audio Preview Double-Muxing Redundancy & Loudness Mismatch: P3 and the active recipe JSON ([recipes/ltx_audio_gguf_tts_dialogue.json](file:///C:/Users/jeffr/Documents/ComfyUI/vram-recipe-lab/recipes/ltx_audio_gguf_tts_dialogue.json#L325-L353)) already implement audio delivery by wiring Node 21 (TrimAudioDuration using gain-matched -25.8 LUFS fixture) into Node 35 (CreateVideo). P5 mandates an external post-render step of "externally muxing the original hash-bound source fixture trimmed to 3.88 seconds." External muxing using the raw, un-gained source fixture (e.g. tts_dialogue.wav at -20.5 LUFS) bypasses P3's -25.8 LUFS frozen loudness invariant, duplicates ComfyUI's internal muxing, and creates a hash discrepancy between ComfyUI's output and the preview artifact. Fix: Amend P5 to specify that ComfyUI's SaveVideo output using the -25.8 LUFS conditioning fixture is the authoritative delivery preview, and restrict external ffmpeg calls strictly to non-destructive stream integrity verification without replacing the audio track.
3. [P7] Unmanaged Model-Cache Accumulation Across Sequential Suite Steps: P7 sequences 11 consecutive recipe executions (W0, S0, T0, T1, S1, I0, I1, S2, R0, R1, S3) on a single live server (127.0.0.1:8199) without an explicit model eviction call between steps. Because h3_t2v_best, h3_i2v_best, and h3_r2v_best use different model components (Gemma text encoder vs Qwen3-VL, FL2VA vs Ref2VA), ComfyUI's default model management retains loaded weights in VRAM across runs. The cumulative VRAM footprint will breach the 14.5 GiB ceiling due to cache accumulation rather than recipe memory creep. Fix: Update P7 to mandate an explicit API call to http://127.0.0.1:8199/free (or comfy.model_management.free_memory) and settled VRAM baseline re-verification before starting each candidate cold step (T0, I0, R0).
4. [P8 / P9] Un-Gated Human Loop Blocks Automated Completion: P8 requires rendering h3_mime_i2v and then states "Stop for Jeffrey. The inverted ear gate is human-only... Do not render R2V mime without that approval." However, P1 and P9 mandate completing docs/SESSION_REPORT.md and local git commit in the same run. Requiring a synchronous human ear gate review mid-pipeline prevents automated script execution from reaching P9. Fix: Restructure P8 and P9 to decouple machine execution from human evaluation: SESSION_REPORT.md must log the machine-rendered Mini Mime artifact hash and record the human ear gate status as PENDING_HUMAN_AUDITION, allowing automated suite execution and local git commit (P9) to complete without stalling.

SHOULD-FIX:
1. [P7] Undefined Suite Warmup Recipe in Plan Narrative: P7 specifies sequence W0, S0, ... and states S* is h3_i2v_low, but leaves W0 recipe identity unstated in the plan body text (though defined in [suites/h3_best_suite.json](file:///C:/Users/jeffr/Documents/ComfyUI/vram-recipe-lab/suites/h3_best_suite.json#L15)). Fix: Explicitly cite suites/h3_best_suite.json in P7 and clarify W0 is h3_i2v_low.
2. [P5] Undefined Media Validity Criteria: P5 mandates stopping the audio matrix on "invalid media", but provides no programmatic definition or threshold for media validity. Fix: Specify explicit verification in P5 (e.g., ffprobe exit code 0, audio/video stream presence, frame count == fps * duration_s).
3. [P2 / P7] Missing Abort Teardown & Lockfile Cleanup Protocol: P2 and P7 mandate immediate abort on VRAM ceiling breaches (>14.5 GiB) or lock failures, but do not specify cleanup handling for .gpu.lock ([run_recipe.py:L31](file:///C:/Users/jeffr/Documents/ComfyUI/vram-recipe-lab/run_recipe.py#L31)) and .suite.lock ([run_h3_suite.py:L34](file:///C:/Users/jeffr/Documents/ComfyUI/vram-recipe-lab/run_h3_suite.py#L34)). An unhandled abort leaves stale locks on disk. Fix: Require explicit try...finally lock release and receipt recording on any early abort signal in run_h3_suite.py and run_recipe.py.

OPTIONAL / NICE-TO-HAVE:
- [P3 / P7] Include total estimated suite execution duration in P7 to help distinguish expected long runs from server hangs.
- [P9] Align section headers in PROMOTION_BRIEF.md with existing documentation standards in docs/.

CUT THESE (scope / over-engineering):
1. [P4 Steps 1-2] Redundant Multi-Stage Pre-Render CLI Review Loops: Requiring a 4-round Kibitz review arc and ephemeral Codex CLI reviews within the pre-render phase of an offline execution runner introduces procedural bloat. Safe to cut: static schema validation via [validate_recipes.py](file:///C:/Users/jeffr/Documents/ComfyUI/vram-recipe-lab/validate_recipes.py) and pytest are already deterministic and sufficient.
2. [P7 Sentinels S1 & S2] Excessive Sentinel Frequency in Warm-Pair Suite: Running h3_i2v_low 4 separate times (S0, S1, S2, S3) within an 11-step sequence to track 0.25 GiB VRAM drift adds unnecessary runtime. Safe to trim S1 and S2 while retaining baseline S0 and final S3 if suite execution time needs optimization.

[ASSUMPTION] Marks:
- [ASSUMPTION] In P7, assuming Windows 11 Desktop Window Manager (DWM) background VRAM fluctuations stay within a <250 MiB threshold during 4-second settled VRAM sampling on an RTX 5080 Laptop GPU.
- [ASSUMPTION] In P4/P7, assuming the local server process on 127.0.0.1:8199 is managed via [boot_lab_server.cmd](file:///C:/Users/jeffr/Documents/ComfyUI/vram-recipe-lab/boot_lab_server.cmd) without external port conflicts.
